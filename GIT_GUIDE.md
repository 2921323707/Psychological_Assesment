# Git 仓库更新指南

## 📋 目录

1. [快速更新方式](#快速更新方式)
2. [手动Git命令](#手动git命令)
3. [常用Git操作](#常用git操作)
4. [脚本使用说明](#脚本使用说明)
5. [故障排除](#故障排除)

---

## 🚀 快速更新方式

### 方式1: 使用Python脚本（推荐）

```bash
python update_github.py
```

**优点**: 
- 跨平台支持（Windows/Mac/Linux）
- 交互式操作
- 自动生成默认提交信息
- 详细的错误提示

### 方式2: 使用Shell脚本（Mac/Linux）

```bash
# 添加执行权限
chmod +x update_github.sh

# 运行脚本
./update_github.sh
```

### 方式3: 使用批处理文件（Windows）

```bash
# 双击运行或在命令行执行
update_github.bat
```

---

## 📝 手动Git命令

### 基本更新流程

#### 1. 查看更改状态
```bash
git status
```

#### 2. 添加文件到暂存区
```bash
# 添加所有更改
git add .

# 添加特定文件
git add app/routes.py

# 添加多个文件
git add file1.py file2.py
```

#### 3. 提交更改
```bash
git commit -m "描述你的更改"
```

**提交信息建议**:
- ✅ 使用中文或英文清晰描述
- ✅ 参考格式: `功能: 简短描述`
- ✅ 示例:
  - `新增: 量表测评界面`
  - `修复: 修复数据库连接问题`
  - `更新: 优化页面布局`
  - `添加: 性压抑指数量表`

#### 4. 推送到GitHub
```bash
git push origin main
```

#### 5. 完整流程示例
```bash
# 1. 查看状态
git status

# 2. 添加所有更改
git add .

# 3. 提交
git commit -m "新增: 完成量表测评功能"

# 4. 推送
git push origin main
```

---

## 🛠 常用Git操作

### 查看历史记录
```bash
# 查看提交历史
git log

# 简洁模式
git log --oneline

# 最近5条记录
git log --oneline -5

# 图形化显示
git log --graph --oneline --all
```

### 撤销操作

#### 撤销未暂存的更改
```bash
# 撤销单个文件
git checkout -- filename.py

# 撤销所有未暂存文件
git checkout -- .
```

#### 撤销已暂存但未提交的更改
```bash
# 移除单个文件
git restore --staged filename.py

# 移除所有暂存文件
git restore --staged .
```

#### 修改最后一次提交
```bash
# 修改提交信息
git commit --amend -m "新的提交信息"

# 添加遗漏的文件
git add forgotten_file.py
git commit --amend --no-edit
```

#### 回退到之前的提交
```bash
# 查看提交历史获取commit hash
git log --oneline

# 回退到指定提交（保留更改）
git reset --soft <commit-hash>

# 回退到指定提交（丢弃更改）
git reset --hard <commit-hash>
```

### 分支操作

#### 创建和切换分支
```bash
# 创建新分支
git branch feature-name

# 切换到分支
git checkout feature-name

# 创建并切换到分支（一条命令）
git checkout -b feature-name

# 查看所有分支
git branch

# 查看远程分支
git branch -r
```

#### 合并分支
```bash
# 切换到主分支
git checkout main

# 合并功能分支
git merge feature-name

# 删除已合并的分支
git branch -d feature-name
```

### 拉取远程更新
```bash
# 从GitHub拉取最新代码
git pull origin main

# 或分步操作
git fetch origin
git merge origin/main
```

### 查看差异
```bash
# 查看工作区与暂存区的差异
git diff

# 查看暂存区与仓库的差异
git diff --staged

# 查看两个提交之间的差异
git diff commit1 commit2
```

### 查看特定文件的更改历史
```bash
git log --follow -- filename.py
```

---

## 📚 脚本使用说明

### update_github.py

**功能**: 自动化的Git更新流程

**使用步骤**:
1. 运行脚本: `python update_github.py`
2. 查看Git状态（自动显示）
3. 确认是否继续（y/n）
4. 输入提交信息（或使用默认）
5. 自动提交和推送

**输出示例**:
```
==================================================
GitHub 仓库更新脚本
==================================================

检查Git状态...
On branch main
Changes not staged for commit:
  modified:   app/routes.py

是否继续更新? (y/n): y

添加所有更改...

提交更改: 新增: 完成量表测评功能...

推送到GitHub...
...

✅ 更新完成！
==================================================
```

---

## 🔧 故障排除

### 问题1: 推送被拒绝
```
error: failed to push some refs to 'origin'
```

**解决方案**:
```bash
# 先拉取远程更新
git pull origin main

# 如果有冲突，解决后重新提交
git add .
git commit -m "解决冲突"
git push origin main
```

### 问题2: 合并冲突
```
CONFLICT (content): Merge conflict in README.md
```

**解决方案**:
1. 打开冲突文件
2. 查找标记: `<<<<<<<`, `=======`, `>>>>>>>`
3. 手动编辑保留需要的代码
4. 保存文件
5. 标记为已解决: `git add 文件名`
6. 完成合并: `git commit`

### 问题3: 忘记添加文件
```bash
# 如果已经commit但忘记添加某个文件
git add forgotten_file.py
git commit --amend --no-edit
git push origin main
```

### 问题4: 提交信息写错
```bash
# 修改最后一次提交信息
git commit --amend -m "正确的提交信息"
git push origin main --force  # 谨慎使用force
```

### 问题5: 本地和远程代码不一致
```bash
# 方法1: 拉取并合并
git pull origin main

# 方法2: 变基（保持历史线性）
git pull --rebase origin main

# 方法3: 强制推送（谨慎使用！）
git push origin main --force
```

---

## 💡 最佳实践

### 1. 提交频率
- ✅ 完成一个功能后立即提交
- ✅ 每天工作结束前提交一次
- ✅ 修复bug后立即提交
- ❌ 不要累积大量更改才提交

### 2. 提交信息规范
```bash
# 好的提交信息
git commit -m "新增: 量表测评界面和逻辑"
git commit -m "修复: 修复数据库连接池问题"
git commit -m "优化: 改进页面加载速度"
git commit -m "文档: 更新API使用说明"

# 不好的提交信息
git commit -m "update"          # 太模糊
git commit -m "fix"             # 不明确
git commit -m "临时保存"        # 没有价值
```

### 3. 分支管理
```bash
main        # 主分支，稳定版本
develop     # 开发分支
feature/*   # 功能分支
hotfix/*    # 紧急修复
```

### 4. 定期同步
```bash
# 每天开始工作前
git pull origin main

# 每天结束前
git add .
git commit -m "更新: 今天的工作内容"
git push origin main
```

---

## 📖 进阶操作

### 查看仓库信息
```bash
# 查看远程仓库
git remote -v

# 查看仓库统计
git shortlog -sn
```

### 暂存更改（临时保存）
```bash
# 暂存当前工作
git stash

# 查看暂存列表
git stash list

# 恢复暂存
git stash pop

# 删除暂存
git stash drop
```

### 打标签（版本标记）
```bash
# 创建标签
git tag -a v1.0.0 -m "版本1.0.0发布"

# 推送标签
git push origin v1.0.0

# 查看所有标签
git tag
```

---

## 🆘 紧急情况处理

### 回到上次提交状态
```bash
# 丢弃所有未提交的更改
git reset --hard HEAD

# 丢弃并拉取最新代码
git fetch origin
git reset --hard origin/main
```

### 撤销已推送的提交
```bash
# 创建新提交撤销更改
git revert HEAD

# 推送撤销
git push origin main
```

---

## 📞 获取帮助

```bash
# 查看Git帮助
git help

# 查看特定命令帮助
git help commit
git help push
git help merge
```

### 在线资源
- [Git官方文档](https://git-scm.com/doc)
- [GitHub指南](https://guides.github.com/)
- [Git教程](https://git-scm.com/book/zh/v2)

---

**仓库地址**: https://github.com/2921323707/Psychological_Assesment

**最后更新**: 2024年

